\documentclass[10pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{titlesec}
\usepackage{hyperref}

\usepackage{minted}

\author{Alexandre Moine}
\title{Rapport de projet}

\newcounter{question}

\titleformat{\subsection}
{\normalfont\normalsize\bfseries}{Question \stepcounter{question}\thequestion}{1em}{}

\begin{document}
\maketitle

\section{Introduction}
% aussi présent pour le numbering

\section{Entrées} % TODO: trouver comment rajouter "appetizers"

\subsection{}
\begin{minted}{coq}
predicate no_3_consecutive_zeros_sub (a:array int) (l:int) =
  forall i. 0 <= i < l-2 -> not (a[i] = a[i+1] = a[i+2] = 0)
\end{minted}
La borne supérieure du prédicat est $l-2$ car en supposant $0 < l <= a.length$, on veut que $i+2 < a.length$.

\subsection{}
\begin{minted}{coq}
predicate no_3_consecutive_zeros (a:array int) =
  no_3_consecutive_zeros_sub a a.length
\end{minted}
En effet, il n'y a pas 3 zéros consécutifs dans $a$ si et seulement s'il n'a pas 3 zéros consécutifs entre les indices $0$ et la taille de $a$.

\subsection{}
\begin{minted}{coq}
for i = 0 to a.length - 3 do
  invariant { no_3_consecutive_zeros_sub a (i+2) }
    if a[i] = 0 && a[i+1] = 0 && a[i+2] = 0 then raise TripleFound
\end{minted}
Les bornes de la boucles sont claires: on s'assure que les indices utiles au test sont valides.\\
Pour l'invariant, on voit que la fonction lève une exception lorsqu'elle trouve 3 zéros consécutifs entre $i$ et $i+2$. À chaque fin de tour, on a donc qu'il n'y a pas 3 zéros consécutifs entre l'indice $0$ et $i+2$.
\subsection{}
\begin{minted}{coq}
for i = 2 to a.length - 1 do
  invariant { last1 = a[i-1] /\ last2 = a[i-2] }
  invariant { no_3_consecutive_zeros_sub a i }
    let v = a[i] in
    if v = 0 && last1 = 0 && last2 = 0 then raise TripleFound;
    last2 <- last1; last1 <- v;
\end{minted}
Les bornes de la boucles sont claires: on s'assure que les indices utiles au test sont valides.\\
Pour l'invariant, on voit que la fonction lève une exception lorsqu'elle trouve 3 zéros consécutifs entre $i-2$ et $i$. À chaque fin de tour, on a donc qu'il n'y a pas 3 zéros consécutifs entre l'indice $0$ et $i$.

\subsection{}
\label{q5}
\begin{minted}{coq}
let rec ghost function num_occ (e:int) (f:int -> int) (i j :int) : int
  variant { j-i }
  = if i>=j then 0 else
    if f (j-1) = e then 1 + num_occ e f i (j-1) else num_occ e f i (j-1)
\end{minted}
On \og parcoure\fg{} la fonction de $j$ à $i$, le variant est donc $i-j$. De plus, sans précondition sur $j$, on donne un test large d'arrêt: $ i >= j$.

\subsection{}

\begin{minted}{coq}
let count_number_of (e:int) (a:array int) : int
  ensures { result = num_occ e (fun i -> a[i]) 0 a.length}  = ...
\end{minted}
On veut s'assurer que la fonction compte bien le nombre d'éléments égaux à $e$ dans tout $a$.

\subsection{}
\begin{minted}{coq}
  let ref n = 0 in
  for i=0 to a.length - 1 do
    invariant { n = num_occ e (fun i -> a[i]) 0 i }
    if a[i] = e then n <- 1+n;
  done; n
\end{minted}
L'invariant est trivial ici: la référence $n$ compte bien le nombre d'éléments égaux à $e$ entre $0$ et $i$.

\subsection{}
\begin{minted}{coq}
predicate identical_sub_arrays (a:array int) (o1 o2 l:int)
= forall k:int. 0 <= k < l -> a[o1+k] = a[o2+k]
\end{minted}
Deux sous-tableaux sont égaux s'ils le sont à chaque indices.
\subsection{}
\begin{minted}{coq}
predicate valid_sub_array (a:array int) (o l:int) =
  0 <= o /\ o + l <= a.length

let check_identical_sub_arrays (a:array int) (o1 o2 l:int) : bool
  requires { valid_sub_array a o1 l /\ valid_sub_array a o2 l  }
  ensures { result = True <-> identical_sub_arrays a o1 o2 l }
= try
    for k=0 to l-1 do
      invariant { identical_sub_arrays a o1 o2 k }
      if a[o1+k] <> a[o2+k] then raise DiffFound
  done; ...
\end{minted}

On commence par se donner un prédicat $valid\_sub\_array\ a\ o\ l$ spécifiant que le sous-tableau $a[o... (o+l-1)]$ est valide. On requiert ensuite que les sous-tableau donné en argument soient valides.\\
L'invariant suit encore une fois directement la spécification. Au rang $i$ de la boucle, on est sûr que les deux sous-tableaux $a[o_1...(o_1+i-1)]$ et $a[o_1...(o_2+i-1)]$ sont égaux.

\section{Takuzu}
\subsection{}

\begin{minted}{coq}
predicate no_3_consecutive_identical_elem'
  (g:takuzu_grid) (start incr : int) (l:int) (e:elem) =
    forall k:int. 0 <= k < l - 2 ->
     let acc = acc g start incr in
     not (acc k = acc (k+1) = acc (k+2) = e)

predicate no_3_consecutive_identical_elem (g:takuzu_grid) (start incr:int) (l:int)
  = no_3_consecutive_identical_elem' g start incr l Zero
 /\ no_3_consecutive_identical_elem' g start incr l One
\end{minted}
On se donne en effet d'abord un prédicat $no\_3\_consecutive\_identical\_elem'$ exprimant qu'il n'y a pas 3 éléments consécutifs égaux ) un paramètre $e$ et on instancie ce paramètre ensuite à $Zero$ et à $One$.

\subsection{}
TODO verify

\subsection{}
\begin{minted}{coq}
let rec function num_occ (e:elem) (g:takuzu_grid) (start incr:int) (l:int)
  requires { g.length = 64 /\ valid_chunk start incr }
  requires { 0 <= l <= 8 }
  variant { l }
= if l=0 then 0 else
  if eq (acc g start incr (l-1)) e
  then 1 + num_occ e g start incr (l-1) else num_occ e g start incr (l-1)

let count_number_of (e:elem) (g:takuzu_grid) start incr : int
  requires { g.length = 64 /\ valid_chunk start incr }
  ensures { result = num_occ e g start incr 8 }
  =
  let ref n = 0 in
  for i=0 to 7 do
    invariant { n = num_occ e g start incr i }
    if eq (acc g start incr i) e then n <- n+1
  done; n
\end{minted}

Le code de $num\_occ$ est calqué sur celui de la question \ref{q5}. Ses pré-conditions sont nécessaires à l'accesseur $acc$ et le variant est évident.\\
Les pré-conditions de $count\_number\_of$ sont nécessaires à l'accesseur $acc$ et l'invariant est trivial.

\subsection{}
\begin{minted}{coq}
predicate rule_2_for_chunk (g:takuzu_grid) (start incr:int) =
  num_occ Zero g start incr 8 <= 4 /\ num_occ One g start incr 8 <= 4

let check_rule_2_for_chunk (g:takuzu_grid) start incr : unit
  requires { g.length = 64 /\ valid_chunk start incr }
  ensures { rule_2_for_chunk g start incr }
  raises { Invalid -> not (rule_2_for_chunk g start incr) } =
  if count_number_of Zero g start incr > 4 || count_number_of One  g start incr > 4
  then raise Invalid
\end{minted}

Le prédicat est calqué sur la définition de la deuxième règle.
Les préconditions de $check\_rule\_2\_for\_chunk$ sont celles de $count\_number\_of$. La post-condiiton et la post-condition exceptionnelle sont triviales à la vue de la spécification de $count\_number\_of$.

\subsection{}
\begin{minted}{coq}
predicate identical_chunks (g:takuzu_grid) (s1 s2:int) (incr:int) (l:int)
= forall k. 0 <= k < l ->
    acc g s1 incr k = acc g s2 incr k /\ acc g s1 incr k <> Empty

let check_identical_chunks g start1 start2 incr : bool
  requires { g.length = 64 /\ valid_chunk start1 incr /\ valid_chunk start2 incr }
  ensures { result = True <-> identical_chunks g start1 start2 incr 8 }
= try for i=0 to 7 do
    invariant { identical_chunks g start1 start2 incr i }
    match acc g start1 incr i, acc g start2 incr i with
    | Zero,Zero | One,One -> ()
    | _ -> raise DiffFound
    end done; ...
\end{minted}

TODO

\subsection{}
TODO size

\subsection{}
\begin{minted}{coq}

predicate rule_1_for_cell (g:takuzu_grid) (n:int) =
  let cs = column_start_index n in
  let rs = row_start_index n in
  rule_1_for_chunk g rs 1 /\ rule_1_for_chunk g cs 8

predicate rule_2_for_cell (g:takuzu_grid) (n:int) =
  let ... in
  rule_2_for_chunk g rs 1 /\ rule_2_for_chunk g cs 8

predicate rule_3_for_cell (g:takuzu_grid) (n:int) =
  let ... in
  forall i. 0 <= i < 8 ->
    (i*8 <> rs -> not (identical_rows g rs (i*8)))
    /\ (i <> cs -> not (identical_columns g cs i))
\end{minted}

TODO

\subsection{}
\begin{minted}{coq}
let check_at_cell (g:takuzu_grid) (n:int) : unit
  requires { g.length = 64 /\ 0 <= n < 64 }
  ensures { valid_for_cell g n }
  raises { Invalid -> not (valid_for_cell g n) } = ...
\end{minted}

Avec tout le travail précédent, $check\_at\_cell$ est simple à spécifier: on requiert que la grille et l'argument soient raisonnables, on assure en sortie que la grille si et seulement si l'exception n'a pas été levée.

\subsection{}
\begin{minted}{coq}
let check_cell_change (g:takuzu_grid) (n:int) (e:elem) : unit
  requires { g.length = 64 /\ 0 <= n < 64 }
  requires { valid_up_to (g[n<-Empty]) n }
  writes { g }
  ensures { valid_up_to g (n+1) }
  ensures { array_eq g (old g)[n <- e]} (* Added for solve_aux *)
  raises { Invalid -> array_eq g (old g)[n <- e] } (* Added for solve_aux *)
\end{minted}
TODO

\subsection{}
TODO

\subsection{}
TODO

\subsection{}
\begin{minted}{coq}
let rec solve_aux (g:takuzu_grid) (n:int) : unit
  requires { g.length = 64 /\ 0 <= n <= 64 }
  requires { full_up_to g n /\ valid_up_to g n }
  writes { g }
  variant { 64-n }
  ensures { array_eq g (old g) }
  raises { SolutionFound -> extends (old g) g /\ full_up_to g 64 /\ valid_up_to g 64 }
\end{minted}
TODO

\subsection{}
TODO

\subsection{}
TODO

\section{Conclusion}
\subsection{}

\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
